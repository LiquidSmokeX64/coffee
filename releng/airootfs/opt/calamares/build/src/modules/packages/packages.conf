# SPDX-FileCopyrightText: no
# SPDX-License-Identifier: CC0-1.0
#
# The configuration for the package manager starts with the
# *backend* key, which picks one of the backends to use.
# In `main.py` there is a base class `PackageManager`.
# Implementations must subclass that and set a (class-level)
# property *backend* to the name of the backend (e.g. "dummy").
# That property is used to match against the *backend* key here.
#
# You will have to add such a class for your package manager.
# It is fairly simple Python code. The API is described in the
# abstract methods in class `PackageManager`. Mostly, the only
# trick is to figure out the correct commands to use, and in particular,
# whether additional switches are required or not. Some package managers
# have more installer-friendly defaults than others, e.g., DNF requires
# passing --disablerepo=* -C to allow removing packages without Internet
# connectivity, and it also returns an error exit code if the package did
# not exist to begin with.
---
#
# Which package manager to use, options are:
#  - apk         - Alpine Linux package manager
#  - apt         - APT frontend for DEB and RPM
#  - dnf         - DNF, the new RPM frontend
#  - entropy     - Sabayon package manager (is being deprecated)
#  - luet        - Sabayon package manager (next-gen)
#  - packagekit  - PackageKit CLI tool
#  - pacman      - Pacman
#  - pamac       - Manjaro package manager
#  - portage     - Gentoo package manager
#  - yum         - Yum RPM frontend
#  - zypp        - Zypp RPM frontend
#
# Not actually a package manager, but suitable for testing:
#  - dummy       - Dummy manager, only logs
#
backend: pacman

#
# Often package installation needs an internet connection.
# Since you may allow system installation without a connection
# and want to offer OPTIONAL package installation, it's
# possible to have no internet, yet have this packages module
# enabled in settings.
#
# You can skip the whole module when there is no internet
# by setting "skip_if_no_internet" to true.
#
# You can run a package-manager specific update procedure
# before installing packages (for instance, to update the
# list of packages and dependencies); this is done only if there
# is an internet connection.
#
# Set "update_db" to 'true' for refreshing the database on the
# target system. On target installations, which got installed by
# unsquashing, a full system update may be needed. Otherwise
# post-installing additional packages may result in conflicts.
# Therefore set also "update_system" to 'true'.
#
skip_if_no_internet: false
update_db: true
update_system: false

# pacman specific options
#
# *num_retries* should be a positive integer which specifies the
# number of times the call to pacman will be retried in the event of a
# failure.  If it is missing, it will be set to 0.
#
# *disable_download_timeout* is a boolean that, when true, includes
# the flag --disable-download-timeout on calls to pacman.  When missing,
# false is assumed.
#
# *needed_only* is a boolean that includes the pacman argument --needed
# when set to true.  If missing, false is assumed.
pacman:
    num_retries: 10
    disable_download_timeout: true
    needed_only: false

#
# List of maps with package operations such as install or remove.
# Distro developers can provide a list of packages to remove
# from the installed system (for instance packages meant only
# for the live system).
#
# A job implementing a distro specific logic to determine other
# packages that need to be installed or removed can run before
# this one. Distro developers may want to install locale packages
# or remove drivers not needed on the installed system.
# Such a job would populate a list of dictionaries in the global
# storage called "packageOperations" and that list is processed
# after the static list in the job configuration (i.e. the list
# that is in this configuration file).
#
# Allowed package operations are:
#  - *install*, *try_install*: will call the package manager to
#       install one or more packages. The install target will
#       abort the whole installation if package-installation
#       fails, while try_install carries on. Packages may be
#       listed as (localized) names, or as (localized) package-data.
#       See below for the description of the format.
# - *localInstall*: this is used to call the package manager
#       to install a package from a path-to-a-package. This is
#       useful if you have a static package archive on the install media.
#       The *pacman* package manager is the only one to specially support
#       this operation (all others treat this the same as *install*).
# - *remove*, *try_remove*: will call the package manager to
#       remove one or more packages. The remove target will
#       abort the whole installation if package-removal fails,
#       while try_remove carries on. Packages may be listed as
#       (localized) names.
# One additional key is recognized, to help netinstall out:
# - *source*: ignored, does get logged
# Any other key is ignored, and logged as a warning.
#
# There are two formats for naming packages: as a name or as package-data,
# which is an object notation providing package-name, as well as pre- and
# post-install scripts.
#
# Here are both formats, for installing vi. The first one just names the
# package for vi (using the naming of the installed package manager), while
# the second contains three data-items; the pre-script is run before invoking
# the package manager, and the post-script runs once it is done.
#
# - install
#   - vi
#   - package: vi
#     pre-script: touch /tmp/installing-vi
#     post-script: rm -f /tmp/installing-vi
#
# The pre- and post-scripts are optional, but you cannot leave both out
# if you do use the *package* key: using "package: vi" with neither script
# option will trick Calamares into trying to install a package named
# "package: vi", which is unlikely to work.
#
# The pre- and post-scripts are **not** executed by a shell unless you
# explicitly invoke `/bin/sh` in them. The command-lines are passed
# to exec(), which does not understand shell syntax. In other words:
#
#     pre-script: ls | wc -l
#
# Will fail, because `|` is passed as a command-line argument to ls,
# as are `wc`, and `-l`. No shell pipeline is set up, and ls is likely
# to complain. Invoke the shell explicitly:
#
#     pre-script: /bin/sh -c \"ls | wc -l\"
#
# The above note on shell-expansion applies to versions up-to-and-including
# Calamares 3.2.12, but will change in future.
#
# Any package name may be localized; this is used to install localization
# packages for software based on the selected system locale. By including
# the string `LOCALE` in the package name, the following happens:
#
# - if the system locale is English (any variety), then the package is not
#   installed at all,
# - otherwise `$LOCALE` or `${LOCALE}` is replaced by the 'lower-cased' BCP47
#   name of the 'language' part of the selected system locale (not the
#   country/region/dialect part), e.g. selecting "nl_BE" will use "nl"
#   here.
#
# Take care that just plain `LOCALE` will not be replaced, so `foo-LOCALE` will
# be left unchanged, while `foo-$LOCALE` will be changed. However, `foo-LOCALE`
# **will** be removed from the list of packages (i.e. not installed), if
# English is selected. If a non-English locale is selected, then `foo-LOCALE`
# will be installed, unchanged (no language-name-substitution occurs).
#
# The following installs localizations for vi, if they are relevant; if
# there is no localization, installation continues normally.
#
# - install
#   - vi-$LOCALE
#   - package: vi-${LOCALE}
#     pre-script: touch /tmp/installing-vi
#     post-script: rm -f /tmp/installing-vi
#
# When installing packages, Calamares will invoke the package manager
# with a list of package names if it can; package-data prevents this because
# of the scripts that need to run. In other words, this:
#
# - install:
#   - vi
#   - binutils
#   - package: wget
#     pre-script: touch /tmp/installing-wget
#
# This will invoke the package manager three times, once for each package,
# because not all of them are simple package names. You can speed up the
# process if you have only a few pre-scripts, by using multiple install targets:
#
# - install:
#   - vi
#   - binutils
# - install:
#   - package: wget
#     pre-script: touch /tmp/installing-wget
#
# This will call the package manager once with the package-names "vi" and
# "binutils", and then a second time for "wget". When installing large numbers
# of packages, this can lead to a considerable time savings.
#
operations:
  - install:
    - vi
    - vi-${LOCALE}
    - wget
    - binutils
    - adapta-gtk-theme
    - adapta-gtk-theme-${LOCALE}
    - alsa-lib
    - alsa-lib-${LOCALE}
    - alsa-plugins
    - alsa-plugins-${LOCALE}
    - amd-ucode
    - amd-ucode-${LOCALE}
    - apparmor
    - apparmor-${LOCALE}
    - appstream-qt
    - appstream-qt-${LOCALE}
    - arc-gtk-theme
    - arc-gtk-theme-${LOCALE}
    - arc-icon-theme
    - arc-icon-theme-${LOCALE}
    - arch-install-scripts
    - arch-install-scripts-${LOCALE}
    - archinstall
    - archinstall-${LOCALE}
    - archiso
    - archiso-${LOCALE}
    - archivetools
    - archivetools-${LOCALE}
    - archlinux-wallpaper
    - archlinux-wallpaper-${LOCALE}
    - aria2
    - aria2-${LOCALE}
    - ark
    - ark-${LOCALE}
    - b43-fwcutter
    - b43-fwcutter-${LOCALE}
    - base
    - base-${LOCALE}
    - base-devel
    - base-devel-${LOCALE}
    - bash-completion
    - bash-completion-${LOCALE}
    - bash-language-server
    - bash-language-server-${LOCALE}
    - bemenu
    - bemenu-${LOCALE}
    - bemenu-ncurses
    - bemenu-ncurses-${LOCALE}
    - bemenu-wayland
    - bemenu-wayland-${LOCALE}
    - bind
    - bind-${LOCALE}
    - blueman
    - blueman-${LOCALE}
    - bluez-libs
    - bluez-libs-${LOCALE}
    - boost
    - boost-${LOCALE}
    - boost-libs
    - boost-libs-${LOCALE}
    - brltty
    - brltty-${LOCALE}
    - broadcom-wl
    - broadcom-wl-${LOCALE}
    - btrfs-progs
    - btrfs-progs-${LOCALE}
    - cabextract
    - cabextract-${LOCALE}
    - chrony
    - chrony-${LOCALE}
    - clang
    - clang-${LOCALE}
    - clonezilla
    - clonezilla-${LOCALE}
    - cloud-init
    - cloud-init-${LOCALE}
    - cmake
    - cmake-${LOCALE}
    - colord
    - colord-${LOCALE}
    - colord-kde
    - colord-kde-${LOCALE}
    - cronie
    - cronie-${LOCALE}
    - cryptsetup
    - cryptsetup-${LOCALE}
    - cups
    - cups-${LOCALE}
    - darkhttpd
    - darkhttpd-${LOCALE}
    - dbus
    - dbus-${LOCALE}
    - dbus-python
    - dbus-python-${LOCALE}
    - dconf
    - dconf-${LOCALE}
    - ddrescue
    - ddrescue-${LOCALE}
    - dhclient
    - dhclient-${LOCALE}
    - dhcpcd
    - dhcpcd-${LOCALE}
    - diffutils
    - diffutils-${LOCALE}
    - directx-headers
    - directx-headers-${LOCALE}
    - discord
    - discord-${LOCALE}
    - dkms
    - dkms-${LOCALE}
    - dmenu
    - dmenu-${LOCALE}
    - dmidecode
    - dmidecode-${LOCALE}
    - dmraid
    - dmraid-${LOCALE}
    - dnsmasq
    - dnsmasq-${LOCALE}
    - dosfstools
    - dosfstools-${LOCALE}
    - e2fsprogs
    - e2fsprogs-${LOCALE}
    - edk2-shell
    - edk2-shell-${LOCALE}
    - efibootmgr
    - efibootmgr-${LOCALE}
    - efitools
    - efitools-${LOCALE}
    - egl-wayland
    - egl-wayland-${LOCALE}
    - elfutils
    - elfutils-${LOCALE}
    - exfatprogs
    - exfatprogs-${LOCALE}
    - expat
    - expat-${LOCALE}
    - extra-cmake-modules
    - extra-cmake-modules-${LOCALE}
    - f2fs-tools
    - f2fs-tools-${LOCALE}
    - fatresize
    - fatresize-${LOCALE}
    - firefox
    - firefox-${LOCALE}
    - firewalld
    - firewalld-${LOCALE}
    - flatpak
    - flatpak-${LOCALE}
    - fsarchiver
    - fsarchiver-${LOCALE}
    - gettext
    - gettext-${LOCALE}
    - giflib
    - giflib-${LOCALE}
    - gimp
    - gimp-${LOCALE}
    - git
    - git-${LOCALE}
    - glib2
    - glib2-${LOCALE}
    - glibc
    - glibc-${LOCALE}
    - glslang
    - glslang-${LOCALE}
    - gnome-color-manager
    - gnome-color-manager-${LOCALE}
    - gnome-keyring
    - gnome-keyring-${LOCALE}
    - gnome-menus
    - gnome-menus-${LOCALE}
    - gnu-free-fonts
    - gnu-free-fonts-${LOCALE}
    - gnutls
    - gnutls-${LOCALE}
    - go
    - go-${LOCALE}
    - gpm
    - gpm-${LOCALE}
    - gptfdisk
    - gptfdisk-${LOCALE}
    - grml-zsh-config
    - grml-zsh-config-${LOCALE}
    - grub
    - grub-${LOCALE}
    - gst-libav
    - gst-libav-${LOCALE}
    - gst-plugin-pipewire
    - gst-plugin-pipewire-${LOCALE}
    - gst-plugins-bad
    - gst-plugins-bad-${LOCALE}
    - gst-plugins-base
    - gst-plugins-base-${LOCALE}
    - gst-plugins-base-libs
    - gst-plugins-base-libs-${LOCALE}
    - gst-plugins-good
    - gst-plugins-good-${LOCALE}
    - gst-plugins-ugly
    - gst-plugins-ugly-${LOCALE}
    - gstreamer
    - gstreamer-${LOCALE}
    - gtk-engine-murrine
    - gtk-engine-murrine-${LOCALE}
    - gtk-update-icon-cache
    - gtk-update-icon-cache-${LOCALE}
    - gtk3
    - gtk3-${LOCALE}
    - gtk4
    - gtk4-${LOCALE}
    - hdparm
    - hdparm-${LOCALE}
    - hicolor-icon-theme
    - hicolor-icon-theme-${LOCALE}
    - hwinfo
    - hwinfo-${LOCALE}
    - hyperv
    - hyperv-${LOCALE}
    - innoextract
    - innoextract-${LOCALE}
    - intel-ucode
    - intel-ucode-${LOCALE}
    - iwd
    - iwd-${LOCALE}
    - j4-dmenu-desktop
    - j4-dmenu-desktop-${LOCALE}
    - jdk-openjdk
    - jdk-openjdk-${LOCALE}
    - jfsutils
    - jfsutils-${LOCALE}
    - jre-openjdk
    - jre-openjdk-${LOCALE}
    - jre-openjdk-headless
    - jre-openjdk-headless-${LOCALE}
    - kcmutils
    - kcmutils-${LOCALE}
    - kconfig
    - kconfig-${LOCALE}
    - kcoreaddons
    - kcoreaddons-${LOCALE}
    - kde-applications-meta
    - kde-applications-meta-${LOCALE}
    - kdevelop-python
    - kdevelop-python-${LOCALE}
    - kgamma5
    - kgamma5-${LOCALE}
    - ki18n
    - ki18n-${LOCALE}
    - kiconthemes
    - kiconthemes-${LOCALE}
    - kio
    - kio-${LOCALE}
    - kitty-terminfo
    - kitty-terminfo-${LOCALE}
    - knewstuff
    - knewstuff-${LOCALE}
    - kpmcore
    - kpmcore-${LOCALE}
    - kscreen
    - kscreen-${LOCALE}
    - kuserfeedback
    - kuserfeedback-${LOCALE}
    - kvantum
    - kvantum-${LOCALE}
    - lib32-alsa-lib
    - lib32-alsa-lib-${LOCALE}
    - lib32-alsa-plugins
    - lib32-alsa-plugins-${LOCALE}
    - lib32-giflib
    - lib32-giflib-${LOCALE}
    - lib32-gnutls
    - lib32-gnutls-${LOCALE}
    - lib32-gst-plugins-base-libs
    - lib32-gst-plugins-base-libs-${LOCALE}
    - lib32-libjpeg-turbo
    - lib32-libjpeg-turbo-${LOCALE}
    - lib32-libldap
    - lib32-libldap-${LOCALE}
    - lib32-libpng
    - lib32-libpng-${LOCALE}
    - lib32-libpulse
    - lib32-libpulse-${LOCALE}
    - lib32-libva
    - lib32-libva-${LOCALE}
    - lib32-libva-mesa-driver
    - lib32-libva-mesa-driver-${LOCALE}
    - lib32-libxcomposite
    - lib32-libxcomposite-${LOCALE}
    - lib32-libxinerama
    - lib32-libxinerama-${LOCALE}
    - lib32-libxslt
    - lib32-libxslt-${LOCALE}
    - lib32-mesa-vdpau
    - lib32-mesa-vdpau-${LOCALE}
    - lib32-mpg123
    - lib32-mpg123-${LOCALE}
    - lib32-ncurses
    - lib32-ncurses-${LOCALE}
    - lib32-openal
    - lib32-openal-${LOCALE}
    - lib32-opencl-icd-loade-${LOCALE}r
    - lib32-opencl-icd-loader
    - lib32-opencl-mesa
    - lib32-opencl-mesa-${LOCALE}
    - lib32-pipewire
    - lib32-pipewire-${LOCALE}
    - lib32-pipewire-jack
    - lib32-pipewire-jack-${LOCALE}
    - lib32-v4l-utils
    - lib32-v4l-utils-${LOCALE}
    - lib32-vkd3d
    - lib32-vkd3d-${LOCALE}
    - lib32-vulkan-icd-loader
    - lib32-vulkan-icd-loader-${LOCALE}
    - lib32-vulkan-mesa-layers
    - lib32-vulkan-mesa-layers-${LOCALE}
    - lib32-vulkan-radeon
    - lib32-vulkan-radeon-${LOCALE}
    - libclc
    - libclc-${LOCALE}
    - libdrm
    - libdrm-${LOCALE}
    - libelf
    - libelf-${LOCALE}
    - libfido2
    - libfido2-${LOCALE}
    - libglvnd
    - libglvnd-${LOCALE}
    - libjpeg-turbo
    - libjpeg-turbo-${LOCALE}
    - libldap
    - libldap-${LOCALE}
    - libnotify
    - libnotify-${LOCALE}
    - libomxil-bellagio
    - libomxil-bellagio-${LOCALE}
    - libpng
    - libpng-${LOCALE}
    - libpulse
    - libpulse-${LOCALE}
    - libpwquality
    - libpwquality-${LOCALE}
    - libreoffice-fresh
    - libreoffice-fresh-${LOCALE}
    - libsecret
    - libsecret-${LOCALE}
    - libunwind
    - libunwind-${LOCALE}
    - libusb-compat
    - libusb-compat-${LOCALE}
    - libva
    - libva-${LOCALE}
    - libva-mesa-driver
    - libva-mesa-driver-${LOCALE}
    - libva-utils
    - libva-utils-${LOCALE}
    - libva-vdpau-driver
    - libva-vdpau-driver-${LOCALE}
    - libvdpau
    - libvdpau-${LOCALE}
    - libx11
    - libx11-${LOCALE}
    - libxcomposite
    - libxcomposite-${LOCALE}
    - libxdamage
    - libxdamage-${LOCALE}
    - libxinerama
    - libxinerama-${LOCALE}
    - libxml2
    - libxml2-${LOCALE}
    - libxrandr
    - libxrandr-${LOCALE}
    - libxshmfence
    - libxshmfence-${LOCALE}
    - libxslt
    - libxslt-${LOCALE}
    - libxxf86vm
    - libxxf86vm-${LOCALE}
    - linux
    - linux-${LOCALE}
    - linux-atm
    - linux-atm-${LOCALE}
    - linux-firmware
    - linux-firmware-${LOCALE}
    - linux-firmware-marvell
    - linux-firmware-marvell-${LOCALE}
    - linux-headers
    - linux-headers-${LOCALE}
    - llvm
    - llvm-${LOCALE}
    - llvm-libs
    - llvm-libs-${LOCALE}
    - lm_sensors
    - lm_sensors-${LOCALE}
    - lua
    - lua-${LOCALE}
    - lutris
    - lutris-${LOCALE}
    - make
    - make-${LOCALE}
    - man-db
    - man-db-${LOCALE}
    - man-pages
    - man-pages-${LOCALE}
    - mesa
    - mesa-${LOCALE}
    - mesa-utils
    - mesa-utils-${LOCALE}
    - mesa-vdpau
    - mesa-vdpau-${LOCALE}
    - meson
    - meson-${LOCALE}
    - mkinitcpio
    - mkinitcpio-${LOCALE}
    - mkinitcpio-archiso
    - mkinitcpio-archiso-${LOCALE}
    - mkinitcpio-nfs-utils
    - mkinitcpio-nfs-utils-${LOCALE}
    - mlocate
    - mlocate-${LOCALE}
    - modemmanager
    - modemmanager-${LOCALE}
    - mokutil
    - mokutil-${LOCALE}
    - mpg123
    - mpg123-${LOCALE}
    - mtools
    - mtools-${LOCALE}
    - nano
    - nano-${LOCALE}
    - ncurses
    - ncurses-${LOCALE}
    - neofetch
    - neofetch-${LOCALE}
    - net-tools
    - net-tools-${LOCALE}
    - networkmanager
    - networkmanager-${LOCALE}
    - nfs-utils
    - nfs-utils-${LOCALE}
    - nftables
    - nftables-${LOCALE}
    - nm-connection-editor
    - nm-connection-editor-${LOCALE}
    - nmap
    - nmap-${LOCALE}
    - noto-fonts
    - noto-fonts-${LOCALE}
    - noto-fonts-cjk
    - noto-fonts-cjk-${LOCALE}
    - noto-fonts-emoji
    - noto-fonts-emoji-${LOCALE}
    - noto-fonts-extra
    - noto-fonts-extra-${LOCALE}
    - ntfs-3g
    - ntfs-3g-${LOCALE}
    - ntfsprogs
    - ntfsprogs-${LOCALE}
    - ntp
    - ntp-${LOCALE}
    - nullmailer
    - nullmailer-${LOCALE}
    - nvme-cli
    - nvme-cli-${LOCALE}
    - open-iscsi
    - open-iscsi-${LOCALE}
    - open-vm-tools
    - open-vm-tools-${LOCALE}
    - openal
    - openal-${LOCALE}
    - opencl-icd-loade-${LOCALE}r
    - opencl-icd-loader
    - opencl-mesa
    - opencl-mesa-${LOCALE}
    - openconnect
    - openconnect-${LOCALE}
    - opengl-man-pages
    - opengl-man-pages-${LOCALE}
    - openjdk-doc
    - openjdk-doc-${LOCALE}
    - openjdk-src
    - openjdk-src-${LOCALE}
    - openssh
    - openssh-${LOCALE}
    - openvpn
    - openvpn-${LOCALE}
    - oxygen-icons
    - oxygen-icons-${LOCALE}
    - partclone
    - partclone-${LOCALE}
    - pavucontrol-qt
    - pavucontrol-qt-${LOCALE}
    - pcsclite
    - pcsclite-${LOCALE}
    - perl
    - perl-${LOCALE}
    - pipewire
    - pipewire-${LOCALE}
    - pipewire-alsa
    - pipewire-alsa-${LOCALE}
    - pipewire-jack
    - pipewire-jack-${LOCALE}
    - pipewire-pulse
    - pipewire-pulse-${LOCALE}
    - plasma-framework
    - plasma-framework-${LOCALE}
    - plasma-meta
    - plasma-meta-${LOCALE}
    - plasma-wayland-protocols
    - plasma-wayland-protocols-${LOCALE}
    - plasma-wayland-session
    - plasma-wayland-session-${LOCALE}
    - polkit
    - polkit-${LOCALE}
    - polkit-qt5
    - polkit-qt5-${LOCALE}
    - power-profiles-daemon
    - power-profiles-daemon-${LOCALE}
    - ppp
    - ppp-${LOCALE}
    - pptpclient
    - pptpclient-${LOCALE}
    - python-gobject
    - python-gobject-${LOCALE}
    - python-mako
    - python-mako-${LOCALE}
    - python-pyqt5
    - python-pyqt5-${LOCALE}
    - python-pyqt6
    - python-pyqt6-${LOCALE}
    - qemu-guest-agent
    - qemu-guest-agent-${LOCALE}
    - qt5
    - qt5-${LOCALE}
    - qt5-svg
    - qt5-svg-${LOCALE}
    - qt5-tools
    - qt5-tools-${LOCALE}
    - qt5-translations
    - qt5-translations-${LOCALE}
    - qt5-xmlpatterns
    - qt5-xmlpatterns-${LOCALE}
    - qt6
    - qt6-${LOCALE}
    - qt6-imageformats
    - qt6-imageformats-${LOCALE}
    - qt6-multimedia-ffmpeg
    - qt6-multimedia-ffmpeg-${LOCALE}
    - qt6-wayland
    - qt6-wayland-${LOCALE}
    - refind
    - refind-${LOCALE}
    - reflector
    - reflector-${LOCALE}
    - reiserfsprogs
    - reiserfsprogs-${LOCALE}
    - rhythmbox
    - rhythmbox-${LOCALE}
    - rp-pppoe
    - rp-pppoe-${LOCALE}
    - rsync
    - rsync-${LOCALE}
    - ruby
    - ruby-${LOCALE}
    - rxvt-unicode-terminfo
    - rxvt-unicode-terminfo-${LOCALE}
    - samba
    - samba-${LOCALE}
    - sbctl
    - sbctl-${LOCALE}
    - sbsigntools
    - sbsigntools-${LOCALE}
    - screen
    - screen-${LOCALE}
    - sddm
    - sddm-${LOCALE}
    - sddm-kcm
    - sddm-kcm-${LOCALE}
    - sdparm
    - sdparm-${LOCALE}
    - seatd
    - seatd-${LOCALE}
    - sg3_utils
    - sg3_utils-${LOCALE}
    - shellcheck
    - shellcheck-${LOCALE}
    - shotwell
    - shotwell-${LOCALE}
    - smartmontools
    - smartmontools-${LOCALE}
    - sof-firmware
    - sof-firmware-${LOCALE}
    - solid
    - solid-${LOCALE}
    - squashfs-tools
    - squashfs-tools-${LOCALE}
    - steam
    - steam-${LOCALE}
    - sudo
    - sudo-${LOCALE}
    - sway
    - sway-${LOCALE}
    - swaybg
    - swaybg-${LOCALE}
    - swayidle
    - swayidle-${LOCALE}
    - swayimg
    - swayimg-${LOCALE}
    - swaylock
    - swaylock-${LOCALE}
    - syslinux
    - syslinux-${LOCALE}
    - system-config-printer
    - system-config-printer-${LOCALE}
    - systemd
    - systemd-${LOCALE}
    - systemd-resolvconf
    - systemd-resolvconf-${LOCALE}
    - tcpdump
    - tcpdump-${LOCALE}
    - terminus-font
    - terminus-font-${LOCALE}
    - tesseract-data-eng
    - tesseract-data-eng-${LOCALE}
    - testdisk
    - testdisk-${LOCALE}
    - texinfo
    - texinfo-${LOCALE}
    - thunderbird
    - thunderbird-${LOCALE}
    - tmux
    - tmux-${LOCALE}
    - tpm2-tss
    - tpm2-tss-${LOCALE}
    - ttf-liberation
    - ttf-liberation-${LOCALE}
    - udev
    - udev-${LOCALE}
    - udftools
    - udftools-${LOCALE}
    - ufw
    - ufw-${LOCALE}
    - unrar
    - unrar-${LOCALE}
    - unzip
    - unzip-${LOCALE}
    - usb_modeswitch
    - usb_modeswitch-${LOCALE}
    - usbmuxd
    - usbmuxd-${LOCALE}
    - usbutils
    - usbutils-${LOCALE}
    - v4l-utils
    - v4l-utils-${LOCALE}
    - valgrind
    - valgrind-${LOCALE}
    - vim
    - vim-${LOCALE}
    - virtualbox
    - virtualbox-${LOCALE}
    - virtualbox-guest-utils
    - virtualbox-guest-utils-${LOCALE}
    - virtualbox-host-modules-arch
    - virtualbox-host-modules-arch-${LOCALE}
    - vkd3d
    - vkd3d-${LOCALE}
    - vlc
    - vlc-${LOCALE}
    - vpnc
    - vpnc-${LOCALE}
    - vulkan-icd-loader
    - vulkan-icd-loader-${LOCALE}
    - vulkan-mesa-layers
    - vulkan-mesa-layers-${LOCALE}
    - vulkan-radeon
    - vulkan-radeon-${LOCALE}
    - w3m
    - w3m-${LOCALE}
    - wine
    - wine-${LOCALE}
    - wine-gecko
    - wine-gecko-${LOCALE}
    - wine-mono
    - wine-mono-${LOCALE}
    - winetricks
    - winetricks-${LOCALE}
    - wireless-regdb
    - wireless-regdb-${LOCALE}
    - wireless_tools
    - wireless_tools-${LOCALE}
    - wireplumber
    - wireplumber-${LOCALE}
    - wlroots
    - wlroots-${LOCALE}
    - wofi
    - wofi-${LOCALE}
    - wpa_supplicant
    - wpa_supplicant-${LOCALE}
    - xdg-desktop-portal-kde
    - xdg-desktop-portal-kde-${LOCALE}
    - xdg-utils
    - xdg-utils-${LOCALE}
    - xf86-video-amdgpu
    - xf86-video-amdgpu-${LOCALE}
    - xfconf
    - xfconf-${LOCALE}
    - xfsprogs
    - xfsprogs-${LOCALE}
    - xl2tpd
    - xl2tpd-${LOCALE}
    - xorg
    - xorg-${LOCALE}
    - xorg-apps
    - xorg-apps-${LOCALE}
    - xorg-server
    - xorg-server-${LOCALE}
    - xorg-xwayland
    - xorg-xwayland-${LOCALE}
    - xorgproto
    - xorgproto-${LOCALE}
    - xreader
    - xreader-${LOCALE}
    - yaml-cpp
    - yaml-cpp-${LOCALE}
    - yay
    - yay-${LOCALE}
    - zenity
    - zenity-${LOCALE}
    - zsh
    - zsh-${LOCALE}
    - zstd
    - zstd-${LOCALE}
  - remove:
    - vi
    - wget
    - binutils
