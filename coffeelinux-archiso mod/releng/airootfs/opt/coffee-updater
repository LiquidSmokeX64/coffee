#!/bin/sh

rm -R /usr/share/wayland-sessions/gnome*
rm -R /usr/share/xsessions/gnome*
#rm -r /mnt/usr/share/xsessions/cinnamon*
pacman -Rdd --noconfirm gnome-terminal gedit
pacman -Syy --noconfirm gnome-console gnome-text-editor
exit
